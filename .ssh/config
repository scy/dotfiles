# Enable automatic connection multiplexing by default.
# %C is only available in OpenSSH 6.7 or newer, which is why I'm not using it here yet.
ControlMaster auto
ControlPath ~/.ssh/.control.%n.%p.%r

# Since I want my known_hosts in Git as well, this could be helpful. I'm not sure whether
# it's worth the readability tradeoff though.
HashKnownHosts yes



# Host-specific configuration.



Host github.com
	User git
	# Try to fool some firewalls.
	HostName ssh.github.com
	Port 443
	# They have a lot of IPs and I don't want to spam my known_hosts.
	CheckHostIP no
